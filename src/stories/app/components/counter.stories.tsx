import CounterComponent from "../../../app/counter/page";

const counter = {
  title: "App/Components",
  component: CounterComponent,
};

export default counter;
export const Counter = () => <CounterComponent />;
